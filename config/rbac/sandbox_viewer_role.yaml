# This rule is not used by the project gpu-playground-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to ephemeral.sandbox.gpu-playground.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: gpu-playground-operator
    app.kubernetes.io/managed-by: kustomize
  name: sandbox-viewer-role
rules:
- apiGroups:
  - ephemeral.sandbox.gpu-playground.io
  resources:
  - sandboxes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ephemeral.sandbox.gpu-playground.io
  resources:
  - sandboxes/status
  verbs:
  - get
