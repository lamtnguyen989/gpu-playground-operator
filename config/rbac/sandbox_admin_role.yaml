# This rule is not used by the project gpu-playground-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over ephemeral.sandbox.gpu-playground.io.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: gpu-playground-operator
    app.kubernetes.io/managed-by: kustomize
  name: sandbox-admin-role
rules:
- apiGroups:
  - ephemeral.sandbox.gpu-playground.io
  resources:
  - sandboxes
  verbs:
  - '*'
- apiGroups:
  - ephemeral.sandbox.gpu-playground.io
  resources:
  - sandboxes/status
  verbs:
  - get
