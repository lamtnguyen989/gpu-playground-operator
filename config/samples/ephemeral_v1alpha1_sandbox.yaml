apiVersion: ephemeral.sandbox.gpu-playground.io/v1alpha1
kind: Sandbox
metadata:
  labels:
    app.kubernetes.io/name: gpu-playground-operator
    app.kubernetes.io/managed-by: kustomize
  name: sandbox-sample
spec:
  # TODO(user): Add fields here
